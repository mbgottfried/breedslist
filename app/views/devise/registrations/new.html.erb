<%= render 'layouts/header' %>
<div class="container">
	<div class="page-header center">
		<h1>Breeder Sign Up</h1>
		<h4>Breedslist is the place to list and sell puppies.</h4>
	</div>
	<div class="col-md-8 col-md-offset-2">
		<div class="panel panel-default">


		  <div class="panel-heading center">
		  	<h5>First, let's set up your breeder account details</h5>
		  </div>
		  <div class="panel-body">
		    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
		      <%= devise_error_messages! %>

		      <div class="form-group">
		        <%= f.label :name %>
		        <%= f.email_field :name, autofocus: true, class: "form-control" %>
		      </div>

		      <div class="form-group">
		        <%= f.label :email %>
		        <%= f.email_field :email, autofocus: true, class: "form-control" %>
		      </div>

		      <div class="form-group">
		        <%= f.label :password %>
		        <%= f.password_field :password, class: "form-control" %>
		      </div>

		      <div class="form-group">
		        <%= f.submit "Create Breeder Account", class: "btn btn-primary" %>
		        <p>Next, you'll create your Breeder Page.</p>
		      </div>
		    <% end %>
		  </div>

		  <div class="panel-footer">
		    <%= render "devise/shared/links" %>
		  </div>
		</div>
	</div>
</div>

<%= render 'layouts/footer' %>